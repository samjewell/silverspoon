.row
  .col-sm-12.text-xs-center.bootstrap-welcome
    %h1
      Welcome #{current_user.email}
    %p= link_to 'edit profile', edit_user_registration_path(current_user)
    %p= link_to 'logout', eval("destroy_#{current_user.type.downcase}_session_path"), method: :delete
